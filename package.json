{
  "name": "node-phash",
  "version": "1.0.0",
  "description": "node wrapper for pHash",
  "main": "index.js",
  "author": "Tim Stableford",
  "license": "MIT",
  "gypfile": true,
  "scripts": {
    "build": "node-gyp rebuild",
    "clean": "node-gyp clean",
    "fix": "./format.sh && eslint --fix *.js"
  },
  "devDependencies": {
    "eslint": "^6.5.1",
    "node-gyp": "^6.0.0"
  },
  "dependencies": {
    "node-addon-api": "^1.7.1"
  }
}
